# --- Orchestration ---
# Used for one reason: checkpointed state, so a run killed by a rate limit resumes
# instead of discarding completed research. See docs/architecture.md.
langgraph>=1.2,<2
langgraph-checkpoint-sqlite>=3,<5

# --- LLM providers (both free tier) ---
groq>=0.11.0
google-genai>=0.3.0

# --- Search (Tavily free tier, DDG needs no key) ---
tavily-python>=0.5.0
ddgs>=6.0.0

# --- Fetch + boilerplate removal ---
httpx>=0.27.0
trafilatura>=1.12.0

# --- Retrieval: ONNX embeddings (~130MB, no torch) ---
fastembed>=0.4.0
numpy>=1.26.0

# --- Schemas, CLI, config ---
pydantic>=2.9.0
typer>=0.12.0
rich>=13.7.0
python-dotenv>=1.0.0

# --- Web app (optional: the CLI works without these) ---
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
streamlit>=1.40.0
